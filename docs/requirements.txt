# Pinned, whole tree. The docs toolchain must not change what it renders
# without somebody choosing to change it, so this is a full freeze of the
# environment the site was last built and reviewed with — not just the top
# level package. Regenerate with:
#
#   python3 -m venv .venv
#   .venv/bin/pip install mkdocs-material==<new version>
#   .venv/bin/pip freeze | grep -v '^pip==' | sort
#
# and rebuild locally before committing the result.
babel==2.18.0
backrefs==8.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
colorama==0.4.6
ghp-import==2.1.0
idna==3.19
Jinja2==3.1.6
Markdown==3.10.3
MarkupSafe==3.0.3
mergedeep==1.3.4
mkdocs==1.6.1
mkdocs-get-deps==0.2.2
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
packaging==26.3
paginate==0.5.7
pathspec==1.1.1
platformdirs==4.11.5
Pygments==2.21.0
pymdown-extensions==11.0.2
python-dateutil==2.9.0.post0
PyYAML==6.0.3
pyyaml_env_tag==1.1
requests==2.34.2
six==1.17.0
urllib3==2.7.0
watchdog==6.0.0
