.superpowers/
bin/
obj/
*.user
.dotnet/
artifacts/

# CI build staging and NuGet package cache
dist/
.nuget/

# The documentation site MkDocs builds. Generated from docs/site/; GitLab
# Pages publishes it from the job's artifact, so it is never committed.
/public/

# Licence signing keys. The issuing tool refuses to write one inside a git
# working tree at all (the licence tool refuses outright); this is the
# second line of defence, because a private key that reaches a commit has to
# be treated as leaked and every licence issued with it reissued.
licence-signing-key.private.json
*.private.json
*.pem
*.p12
*.pfx

# The issuing tool's ledger of who was given a licence for which server. It is
# a record of credentials, so it follows the private key's rule: the tool
# refuses to write it inside a git working tree, and this is the second line of
# defence. It lives beside the signing key, outside any repository.
licences-issued.jsonl

# Service configuration: the example is committed, a real one never is.
service/.env
.env

# The internal development record - specs, plans, spikes, verification notes.
# It is working material, not documentation, and it named this network's own
# hosts. The repository is mirrored publicly, so it lives outside it now.
docs/superpowers/

# The operator's own runbook: which machine is which, which key is where. It is
# not secret in the way a key is, but it maps this network, and the repository
# is mirrored publicly. The same arrangement written in roles rather than host
# names is committed, in docs/site/signing-a-release.md.
RUNBOOK.local.md

# The licence tool. It mints licences and signs releases: it is the vendor's
# own program, it is of no use to anybody who cannot already sign, and this
# repository is mirrored publicly. It stays on the machines that hold the keys.
#
# IT IS NOT BACKED UP BY BEING HERE ANY MORE. Keep it wherever the keys are
# kept, and note that service/tests LicenceToolCompatibilityTests can only
# check the tool for drift in a checkout that has it - elsewhere those tests
# skip and say so.
tools/
